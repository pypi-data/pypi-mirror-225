[tool.poetry]
name = "pattern-tiler"
version = "0.1.0"
description = ""
authors = ["Diego Canez <canezdiego@gmail.com>"]
readme = "README.md"
packages = [{include = "pattern_tiler"}]

[tool.poetry.dependencies]
python = "^3.9"
pillow = "^10.0.0"


[tool.poetry.group.dev.dependencies]
flake8 = "^6.1.0"
black = "^23.7.0"
isort = "^5.12.0"

[tool.poetry.scripts]
pattern-tiler = "pattern_tiler.tile:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
