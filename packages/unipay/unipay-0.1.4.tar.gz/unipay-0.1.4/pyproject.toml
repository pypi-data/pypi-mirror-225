[project]
name = "unipay"
version = "0.1.4"
description = ""
authors = [
    { name = "djkcyl", email = "cyl@cyllive.cn" },
]
dependencies = [
    "fastapi",
    "qrcode[pil]",
    "peewee",
    "uvicorn",
    "jinja2",
]
requires-python = ">=3.8"
readme = "README.md"

[project.license]
text = "MIT"

[project.scripts]
unipay = "unipay.__main__:main"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm.build]
includes = [
    "unipay",
]
