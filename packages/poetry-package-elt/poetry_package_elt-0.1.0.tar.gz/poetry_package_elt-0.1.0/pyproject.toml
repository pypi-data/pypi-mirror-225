[tool.poetry]
name = "poetry-package-elt"
version = "0.1.0"
description = "logic of API requests"
authors = ["Burhan523 <burhanuddin@mentorskool.com>"]
readme = "README.md"
packages = [{include = "poetry_package_elt"}]

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.31.0"
pandas = "^2.0.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
