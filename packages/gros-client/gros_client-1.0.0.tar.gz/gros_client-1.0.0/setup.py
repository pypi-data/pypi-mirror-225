from setuptools import setup

setup(
      name='gros_client',  # 需要打包的名字,即本模块要发布的名字
      version='1.0.0',  # 版本
      description='init',
      author='fftai',
      author_email='software@fftai.com',
      # requires=['requests','urllib3'], # 依赖包,如果没有,可以不要
      license='MIT'
)
