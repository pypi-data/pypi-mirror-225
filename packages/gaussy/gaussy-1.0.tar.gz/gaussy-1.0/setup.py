from setuptools import setup

setup(name='gaussy',
      version='1.0',
      description='Gaussian distributions',
      packages=['gaussy'],
      author = 'Evalyne Ndanu Musili',
      zip_safe=False)
