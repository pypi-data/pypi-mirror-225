[tool.poetry]
name = "import-transforms"
version = "0.6.0"
description = ""
authors = ["Adam Hupp <adam@hupp.org>"]
license = "MIT"
readme = "README.md"
packages = [{ include = "import_transforms" }]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.0"
black = "^23.7.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
